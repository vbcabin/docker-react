language:  generic
sudo:  required
services:
  - docker

before_install:
  - docker build -t vbcabin/docker-react -f Dockerfile.dev .

script:
  - docker run -t vbcabin/docker-react npm run test -- --coverage

